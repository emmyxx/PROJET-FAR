/**
 * @brief Convertit une chaîne de caractères en un message formaté.
 * @param saisie Le message à formater.
 * @return Un pointeur vers le message formaté, ou NULL si le message ne peut pas être formaté.
 * @note L'appelant doit libérer la mémoire allouée par cette fonction.
 */
void *formater(const char *saisie);
